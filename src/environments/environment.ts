/**
 * @description Configuration for the development environment.
 * In a real application, these values would be different in environment.prod.ts.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api' // Example API URL for local development
};
